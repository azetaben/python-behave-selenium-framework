# Config Package

This package is the canonical runtime import path for framework configuration.

Use these imports in tests and framework code:

```python
from config.config import settings
from config.integration import build_config_bundle
```

Legacy imports under `doc.config` remain available for backward compatibility.

